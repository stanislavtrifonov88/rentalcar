//0.1
export const ageDiscount = (c: ContractData) :number => {
  return 0;
}

//-0.1
export const locationDiscount = (c: ContractData) :number => {
  return 0;
}

//+0.2
export const loyaltyDiscount = (c: ContractData) :number => {
  return 0;
}

//+0.2
export const wheatherDiscount = (c: ContractData) :number => {
  return 0;
}


export default [ageDiscount, locationDiscount, loyaltyDiscount, wheatherDiscount]